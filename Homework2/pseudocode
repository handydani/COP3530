

stack operand
stack operator
cin >> state
while (state)
    state: operand
      operand.push(operand)

    state: operator
      operator.push(operator)

    cin >> state
end

while (operand)
    a = operand.pop
    b = operand.pop
    result = 0
    x = operator.pop

    (x)
        state +
          result += a + b
        state -
          result -= a - b
        state *
          result *= a * b
        state /
          result /= a / b
end

return result
