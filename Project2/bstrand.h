template <typename K>
template <typename V>
template <typename C>
template <typename E>
insert( key, value ) — adds the specified key-value pair to the map.
remove( key ) — removes the specified key-value pair associated with the key.
lookup( key ) — returns a reference to the value associated with the key.
